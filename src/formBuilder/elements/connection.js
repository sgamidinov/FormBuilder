class Connection {
    constructor({form, connections}) {
        this.form = form;
        this.connections = connections;
        this.connectionEvaluators = {};
    }

    addConnection(connection) {

    }

    removeConnection(connection) {

    }

    makeConnection() {
        for (const connection of this.connections) {

        }
    }
}

export default Connection;
